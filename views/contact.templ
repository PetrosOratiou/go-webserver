package views

templ Contact(hxReq bool) {
    if !hxReq {
        @Base("Contact", contact())
    } else {
        @contact()
    }
}

templ contact() {
    <h1>Contact</h1>
    <p>Work in progress. Come back later! 🛠️</p>
}